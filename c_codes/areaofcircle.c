#include <stdio.h>
#define pi 3.14
void main()
{
int r,area;
    printf("enter the radius");
    scanf("%f", &r);

    return (0);
}

aofc()
{
    int r, area;
    area = pi * r * r;
    printf(" the area of circle is %f", area);
    
}