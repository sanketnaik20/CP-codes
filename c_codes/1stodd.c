#include <stdio.h>
int main()
{
    int i = 1, n;
    printf("enter numbers to print");
    scanf("%d", &i);
    while (i <= n)
    {

        printf("%d\n", 2 * n - 1);
        i++;
    }
    return 0;
}